var b;

function setup() {
	createCanvas(400,400);
	b = new Ball();
}

function draw() {
	background(51);
}

function mousePressed(){
	//var dist = Math.sqrt((Math.abs(x-mouseX)^2) + (Math.abs(y-mouseY)^2));
	//console.log(dist);
}